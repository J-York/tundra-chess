{
  "name": "tundra-chess",
  "version": "0.0.0",
  "private": true,
  "description": "苔原远征 · 长夜行旅 — 原生 HTML/CSS/JS 单人自走棋，无构建步骤与运行时依赖",
  "license": "UNLICENSED",
  "type": "commonjs",
  "scripts": {
    "test": "node test.cjs",
    "check": "node --check world.js && node --check engine.js && node --check art.js && node --check audio.js && node --check ui-store.js && node --check ui-effects.js && node --check ui-map.js && node --check ui-board.js && node --check ui-battle.js && node --check ui-dialogs.js && node --check game.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "balance": "node balance.cjs 30",
    "fingerprint": "node fingerprint.cjs",
    "serve": "python3 -m http.server 8879 --bind 127.0.0.1",
    "verify": "npm run check && npm run lint && npm run format:check && npm test && npm run browser-test",
    "browser-test": "node --experimental-websocket browser-test.cjs"
  },
  "devDependencies": {
    "eslint": "^9.39.0",
    "globals": "^16.5.0",
    "prettier": "^3.9.6"
  },
  "engines": {
    "node": ">=20"
  }
}
