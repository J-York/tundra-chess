# 成长节奏与战斗反馈验证

验证日期：2026-09-05。

## 当前规则

`node test.cjs`：40 项检查通过，`world.js`、`engine.js`、`game.js` 语法检查通过。

保留战斗 / 经济 / 存档的完整回归，其中包含 75 组随机战斗、100 个种子地图连通性与营地可达性检查。本次增加战败后各章收入和两次免费刷新、酒馆对子成长位、前两章敌群增长、攻击目标与嘲讽原因、承伤分类与阵亡记录、同一快照恢复、旧记录兼容和报告证据检查。

实际承伤按生命损失记，护盾吸收另计；首位阵亡按记录时间排序，守卫存活按守卫职业平均，存活者以结算时间截断。旧战斗没有完整字段时不伪造 0 值。27 站完整规则流程使用加强阵容，只证明流程可达，不用于平衡性结论。

## 修正模拟策略后再比较

发现旧 `balance.cjs` 会在同种伙伴达到三张时停止购买，即使实际是「一张二星 + 两张一星」也无法再买第三张一星。这是测试策略缺陷，造成后期升星停滞。已移除该错误限制，并加入可重现的回归检查。

因此，先前低通关率不能作为英雄弱势的直接证据。下面两组都使用修正后的同一策略；没有注入额外金币、伙伴或星级。

`node balance.cjs 30`：每组为三难度 × 三开局 × 30 种子，种子 `1000 + i * 37`。

| 难度 | 开局 | 调整前规则 | 当前规则 |
| ---- | ---- | ---------: | -------: |
| 悠游 | 林地 |    30 / 30 |  30 / 30 |
| 悠游 | 星辉 |    30 / 30 |  30 / 30 |
| 悠游 | 月影 |    30 / 30 |  30 / 30 |
| 冒险 | 林地 |    29 / 30 |  29 / 30 |
| 冒险 | 星辉 |    28 / 30 |  29 / 30 |
| 冒险 | 月影 |    30 / 30 |  30 / 30 |
| 险境 | 林地 |    10 / 30 |   8 / 30 |
| 险境 | 星辉 |    15 / 30 |  16 / 30 |
| 险境 | 月影 |    18 / 30 |  25 / 30 |

当前规则第一章普通敌群最多四人、精英最多五人，第二章人数与星级错开增长；酒馆成长位优先补齐一星对子；撤退收入 3 / 4 / 5，下一场两次免费刷新。险境敌人倍率从 1.15 调为 1.20，仍保留更高远征损耗。其余英雄基础属性、第三章敌群和首领阵容没有为追求某个胜率而修改。

这是一套使用所有正常接口、穷举当前持有伙伴组合且偏爱开局阵营的启发式策略，结果不是人类玩家胜率。林地险境仍难于月影；真人操作、转型、选路与装备取舍尚需试玩反馈。不能据此宣称所有阵容等强。

## 实际浏览器交互

所有本地场景使用隔离内存存储，不覆盖玩家的远征。

- 实际选择法师，切换技能预览并点击敌方第 8 格：显示以其为中心的五格范围，同时保留金色普攻射程；选择与范围模式保留。
- 实际将骑士的圆盾换为长靴：比较生命 302→270、护甲 44→26、攻击间隔 1.45→1.21 秒、移动间隔 0.36→0.29 秒；穿戴成功，圆盾返回行囊，存档有效。
- 战斗中选中骑士，连线与敌人高亮指向实际交战目标，面板显示选择原因；修正连线 SVG 坐标尺度后截图确认可见。
- 暂停时不再遮挡或模糊棋盘，暂停状态、时间和继续按钮仍可见。刷新后的存活人数按战斗快照展示。
- 真实模拟产生的战败报告：游侠 4.5 秒首亡、守卫存活 5.8 秒，最后一击来自刺客；报告据此提示保护后排。敌我切换与承伤页正常。
- 实际从战败报告返回地图，进入相连普通战：显示两次免费刷新；用完后显示 2 金币刷新，免费操作不扣金币。
- 旧报告不显示承伤按钮，明确告知未记录阵亡时间与承伤类型；规则校验通过。
- 1366×768 战报、1024×640 战斗与技能预览、390×844 战报与装备比较经截图检查。1024×640 文档宽高等于视口，桌面仍为一屏；手机文档无横向溢出。
- 战报正文内部滚动，继续按钮固定在可见区域；手机实测按钮位于视口内。浏览器无警告或错误。

本轮未重跑原有声音专项；音频实现未改动。当前测试页面为 `tests/feedback-qa.html`，可选 phase=prep / battle / win / loss / legacy，不发布到公网。

## 发布

发布文件和域名校验见 DEPLOYMENT.md。公网 8 个文件 HTTPS 200 且与本地 SHA-256 一致。本次 ego-browser 实际打开公网首页、地图及伙伴面板通过，新酒馆说明、23 个章节点及技能范围按钮可见；公网验收没有改动远征进度。完整战斗与战后操作在本地隔离页面验证。

## 十二伙伴 · 2026-09-05

- 45 项规则检查通过；108 局完整远征模拟正常结束，各开局与难度覆盖。
- ego-browser 实际验证十二图鉴、招募合成二星、六格技能预览、战斗技能日志与凋零状态；390px 无横向溢出，页面无错误。
- 桌面截图 /tmp/tundra-heroes-desktop.png 已检查；手机 CLI 截图受 headless 最小宽度影响，仅作形象参考，手机布局采用实际 390px DOM 测量验证。
- ego 截图接口失败，使用隔离 Chrome 完成桌面视觉检查。命令行域名访问被办公策略拦截，正常浏览器访问成功；八个公网文件经浏览器 SHA-256 与本地一致。
- 发布 20260905-162728-heroes；保留 20260905-155338-feedback。

## 构筑组合 · 2026-09-05

- 52 项规则检查通过，108 局完整远征模拟结束。护盾各层独立过期，伤害、破盾和读档保留一致性。
- 桌面与 390px 手机构筑手记截图均已验收，无横向溢出。四组条件、九件装备说明、战斗触发日志与结算触发统计已在浏览器验证。
- 发布 20260905-164532-builds；八个公网资源 HTTPS 200 且 SHA-256 相同；保留 heroes 版本。

## 商人与事件 · 2026-09-05

- 55 项检查通过：推荐商品固定、资源支出、指定奖励、拒绝交易、两层遗物上限、保留精制与穿戴装备、存档一致性。108 局模拟结束。
- 桌面商人和手机事件截图验收通过，实际浏览器购买 7 金币装备、10 生命+4 金币契约、3 金币+备用装备改造均正确。
- 修复点击等待音频解锁导致操作延迟，挂起音频时交易仍立即响应。
- 20260905-165305-choices 已发布。源站所有哈希一致，公网脚本/样式/图片哈希一致，首页仅额外添加 Cloudflare 统计脚本。发布验证必须等待 Network.loadingFinished，提前读图会得到不完整 PNG。

## 种子与远征记录 · 2026-09-05

- 种子分享版 58 项检查，手机卡片溢出修复；输入、取消、剪贴板失败时手动复制经浏览器验证。
- 远征记录与挑战版 60 项规则检查；36 局跨挑战、难度和开局的完整模拟均正常结束。该小样本只验证流程，不作为真人胜率。
- 四人上限、无利息、禁止装备在规则层生效，存档与分享链接拒绝非法配置；旧存档未指定挑战时沿用标准规则。
- 实际浏览器验证记录生成、阵容回看、用种子 678 再出发并选择赤手守望；已有记录保留、页面无错误。390×844 手机记录截图无横向溢出。
- 最近 30 次已结束远征保存在当前浏览器；旧累计凯旋数保留，旧阵容不伪造。

## 棋子与战斗表现 · 2026-09-05

- 角色渐变、描边、雕刻底座与星级轮廓统一；攻击方向动作、施法动作及坠星/冰霜/鳞粉/破盾等独立标记接入。
- 修正守卫护盾施法光圈出现在敌人位置的问题；治疗光圈使用实际治疗目标。
- 动态效果使用可暂停动画；原箭雨延时不再使用跨战斗的定时器。浏览器正常动态模式实测暂停后 9 个动画均 paused，0.5 秒后时间仍为 0 且 2 个光效仍在；三倍速恢复 0.7 秒推进战斗 2.1 秒。
- 减少动态效果清除当前光效，新投射物/光圈/飘字均不生成；同时尊重系统设置。1366×768 和 390×844 截图验收无横向溢出。

## 资源优化与最终审计进展

- 2026-09-05：60 项规则检查及 108 局三开局×三难度完整远征模拟通过，无非终止流程或非法状态。特殊挑战另有上一轮 36 局覆盖。
- forest.webp 保留 1536×1024 分辨率，340820 字节，原图约 2.7 MB；转码后已目视检查。尚未取得新的章节场景图。

- 资源优化版最终发布 20260905-174231-assets-utf8，八个公网资源完整加载并哈希一致。中间候选CSS差异精确对应Windows-1252解码；加入@charset UTF-8后严格哈希通过。

## 2026-09-05 · 22 位伙伴扩充验收

- `node test.cjs`：69 项通过。新增十位的招募、升星、敌群、技能真实目标与效果、双阵营去重、职业羁绊、战斗快照继续均覆盖；旧十二伙伴快照结果保持一致。
- `node balance.cjs 12`：三档难度 × 三种原开局 × 十二种子，108 局正常结束且状态有效。
- 另做 24 局转型模拟：林地正常开局，采用 balance.cjs 的正常购买、刷新、布阵与推进动作转向潮汐/余烬，各 12 局，无额外金币或角色注入。均形成三种新阵营伙伴羁绊，最终上阵的对应阵营二星以上角色平均 3.5 / 4 位。首次两种羁绊平均第 10.3 / 10.8 站。这是固定策略小样本，不等于真人胜率或全面平衡结论。
- ego-browser 本地隔离测试页：图鉴 22 位、阵营与职业过滤、双阵营文字、羁绊候选替代伙伴与横潮整排预览检查。390×844 视口的文档宽度为 390；“潮汐 + 辅助”仅显示珍珠医者、溪羽琴师，未捕获页面错误。
- 十位新角色均有原创 SVG 外形、技能标记及现有动作接入；1440×1050 全员预览与 390×844 图鉴截图已目视检查。独立测试页和快照不发布到公开站点。
- 本轮没有新增章节手绘场景，之前的图像生成阻塞独立保留。

公网验收：`20260905-211704-companions-22` 八个资源均 200 且哈希一致。实际页面规则标识 companions-22，可招募角色数 22；通过图鉴输入事件选择余烬 + 刺客，只显示星火斥候，说明标记余烬 / 星辉 / 刺客。关闭弹窗前后 phase 均为 prep，没有修改远征进度。

## 2026-09-08 · 工程基线、难度曲线与阵营可达性

本轮改动三处：引入开发期检查与统一格式；重新标定三档难度；补齐潮汐 / 余烬的开局与酒馆权重。规则改动只涉及 `DIFFICULTIES.scale`、`ORIGINS` 与酒馆牌池权重，战斗、经济、地图与存档规则未改。

### 格式化的等价性证明

`fingerprint.cjs` 对固定种子的 40 场战斗（逐单位血量、护盾、伤害、治疗、吸收、施法、击杀、阵亡时刻）、60 张种子地图（节点类型、模板、词缀、事件、星级、连接）与 60 局完整远征（结果、站数、剩余生命、战斗场次、星级、收入、遗物）取 SHA-256。

- 格式化前后指纹均为 `f2109b91dc59459b6f53263b628810ee18a8935ac2ab7319a1ed19745ff6ab6a`，逐字节相同，证明 Prettier 只改了排版。
- `index.html`：按浏览器空白折叠规则提取的渲染文本、以及标签与属性流，格式化前后完全一致（HTML5 中 `<meta …>` 与 `<meta … />` 等价）。
- `style.css`：归一化「尾分号 / 引号风格 / `0.18s` 与 `.18s` / 属性选择器加引号」这四类等价写法后，声明序列完全一致。
- 单行长度：`style.css` 由最长 28781 字符降为 109，`engine.js` 由 792 降为 191。`game.js` 仍有 43 行超过 200 字符，全部是 HTML 模板字符串本身，不含逻辑。

### 开发期检查

`npm run verify` = `node --check` × 5 + `eslint .` + `prettier --check .` + `node test.cjs`。ESLint 报出的 6 处真实问题已全部修掉，其中 `eventOptions` 里的 `relic` 是从未被读取的死代码（`lootFor` 无副作用，删除不改变行为，指纹可证）。`game.js` 的顶层全局是 `index.html` 与 `tests/*-qa.html` 依赖的公开接口，因此单独关闭 `no-implicit-globals` 而不是包 IIFE。`deploy.py` 在 `node_modules` 存在时把 lint 与格式检查纳入发布闸门，缺失时跳过并打印提示，保证干净检出仍可发布。

### 难度曲线

改动前（五开局 × 三难度 × 30 种子 = 450 局）：悠游 100%、冒险 97%、险境 57%。冒险作为默认难度几乎不会失败，险境则断崖。

用 `balance.cjs` 的同一组种子（`1000 + i × 37`）扫描敌人强度倍率，每个点 150 局：

| 倍率           | 1.08 | 1.10 | 1.12 | 1.17 | 1.19 | 1.21 |
| -------------- | ---: | ---: | ---: | ---: | ---: | ---: |
| 参考机器人胜率 |  83% |  79% |  75% |  64% |  55% |  49% |

险境另有「有阵亡再扣 1 生命、战败多扣 3」的惩罚，同倍率下比扫描值再低约 6 个百分点，因此单独在真实险境路径上标定（1.15 → 68%、1.17 → 59%、1.19 → 52%）。

最终取悠游 0.94、冒险 1.10、险境 1.19，`node balance.cjs 30` 共 450 局：

| 难度 |       改动前 |        当前 |
| ---- | -----------: | ----------: |
| 悠游 | 150/150 100% | 149/150 99% |
| 冒险 |  145/150 97% | 119/150 79% |
| 险境 |   86/150 57% |  78/150 52% |

界面显示的「−15%」「+8%」由 `scale` 与冒险倍率之比计算得出，不再手写，规则与文案无法脱节。这是固定启发式策略的模拟胜率，不等于真人胜率。

### 阵营可达性

酒馆牌池此前按价格给份数（4 费 2 份、其余 3 份），阵营权重为林地 17、月影 14、星辉 13、潮汐 11、余烬 11——成员最少的两个阵营在相同的 2 / 3 阈值下出现率低约 35%。现按阵营规模补偿：每缺一位成员多进入牌池一次，五个阵营的权重均为 19，测试以「最高 / 最低出现率之比 < 1.15」锁定。该补偿同时把游侠权重由 8 提到 11、刺客由 9 提到 13，这两个只有三名成员的职业羁绊也更容易凑齐。

新增潮汐同舟（潮锚卫士 + 听潮术士，10 金）与余烬行军（炉心重卫 + 火萤弩手，10 金）两个开局，起手价值与既有开局一致（两位起手伙伴费用 + 初始金币 = 15；月影突袭本就是 16，保持不变）。首批酒馆固定含一位同阵营伙伴，五个开局的这项条件由测试统一约束。

五开局 × 30 种子的冒险难度：林地 21、星辉 22、月影 25、潮汐 23、余烬 28；险境：18 / 11 / 18 / 12 / 19。潮汐与星辉在险境偏弱，属于已知差距，本轮不再单独调整数值。

`node test.cjs` 现为 71 项，新增两项分别锁定阵营出现率与开局条件、难度顺序与显示百分比的一致性。改动后的行为指纹为 `cde3c0ae74988a69d0786f52774cce46230db74495d4333a3127320cc17ba665`。

### 浏览器验收

本地隔离 headless Chrome 151（独立 user-data-dir 与调试端口，未使用也未影响机器上已有的浏览器会话），加载 `http://127.0.0.1:8879/`：

- 1440 × 1000：五张开局卡片同一行排列，每张宽 134px，各含两位伙伴立绘；网格未超出对话框，页面无横向滚动。
- 390 × 844：五张卡片按 2 + 2 + 1 排三行，每张宽 150px，无横向溢出。
- 三档难度按钮文案为「敌人强度 −15%」「标准挑战」「敌人强度 +8%」，与 `scale` 计算结果一致。
- 通过真实 UI 路径选择潮汐同舟 + 险境并点击出发：`origin=tide`、`difficulty=hard`、`phase=prep`，起手为潮锚卫士与听潮术士，首批酒馆含珍珠医者，潮汐羁绊计 2，`validate` 通过。
- 全程 `Log.entryAdded` 与 `Runtime.exceptionThrown` 均无记录，控制台零错误。

开局卡片由固定 3 列改为 `auto-fit` 自适应以容纳五个开局；同时删除了一条位于 `@media (max-width: 420px)` 内、但被文件后方同名同权重基础规则遮蔽、实际从未生效的覆盖规则。

### 公网发布

`20260908-064830-balance-and-tooling` 已发布，回退版本 `20260905-211704-companions-22`。八个公网资源全部 HTTPS 200 且 SHA-256 与本地一致，本次无需 Cloudflare 统计脚本归一化。独立复核：公网 `engine.js` 与本地逐字节相同，线上可见五个开局（林地守望 / 群星秘术 / 月影突袭 / 潮汐同舟 / 余烬行军）与新难度倍率 0.94 / 1.1 / 1.19。

发布过程修了 `deploy.py` 的两处问题，两处都不影响线上：

- 公网校验依赖的 `ego-browser` 在当前环境不存在，而该调用位于原子切换之后，缺失时会造成「站点已更新但记录未写」。改为直接 HTTPS 抓取比对摘要；Cloudflare 对默认 `Python-urllib` UA 返回 403，因此带上浏览器 UA，原有的统计脚本归一化保留作兜底。
- 远端写入以 `ops` 身份执行，而 `/personal/tundra-chess` 及历来所有 release 目录均为 root 所有，建目录被拒。写入侧改为 `sudo python3`，只读校验保持普通权限。该次失败发生在切换 `current` 之前，线上全程停留在旧版本。

## 2026-09-08 · 消除数值与文案漂移，扩大地图选择空间

本轮三项改动，前两项语义完全不变（行为指纹逐字节相同），第三项改变地图生成。

### 装备文案由数值生成

精制版此前用正则把描述里所有数字 ×1.6，会连规则固定的数字一起放大——余烬木心的 40% 触发线与 5 秒持续时间都会被算错，因此当年硬写了一段覆盖文案打补丁。现在每件装备声明自己的数值与一个模板，基础版与精制版用同一模板渲染各自的数值，覆盖文案已删除。`SHIELD_SECONDS` 与 `EMERGENCY_HP` 同时供 `hurt()`、`shield()` 和文案使用。18 条装备条目的名称、描述、数值与改动前逐字节相同。

### 技能数值单一来源

`cast()` 曾是 30 分支的内联字面量，而每位伙伴的描述把同样的数值再用中文复述一遍，没有任何检查保证 `atk * 2.3` 与「230% 攻击」一致。现在每个技能把数值放在 `SKILLS[type].params`，`cast()` 读取它，描述由同一对象渲染。共享常量（护盾持续、嘲讽范围、刺客潜伏与影幕窗口、应急护盾触发线、法力上限）统一命名并双向使用。

三条检查守住这个性质：描述必须等于参数渲染的结果；改动任一参数，描述必须随之改变；`cast()` 中不得出现 0、1、星铃周期 3 与齐射动画延迟 0.07 之外的数字字面量。

第二条检查当场发现两个真问题：`healer.targets` 驱动 `.slice(0, targets)` 而文案写的是汉字「两名」，改数量不会改文案；`frost.radius` 则从未出现在文案里。前者改为由参数渲染汉字数量，后者回归内联邻接，与 oakmaul、emberguard 既有的「相邻」写法一致。23 条技能描述与改动前逐字节相同。

### 地图选择空间

九层中有三层是固定形态：第 4 层中路恒为商人、第 7 层恒为营地/战斗/营地、第二条路只有 72% 概率出现。300 个种子的统计，改动前 → 改动后：

| 指标                       |          改动前 |          改动后 |
| -------------------------- | --------------: | --------------: |
| 中段（1–6 层）无分支的节点 |           25.6% |              0% |
| 没有战斗的层               |             128 |               0 |
| 首领前一层的形态种类       |               1 |               3 |
| 商人位置                   | 固定第 4 层中路 | 第 3–5 层任意道 |

同时修掉一个此前静默失效的保证：「每层都有战斗」的检查跑在强制放置**之前**，第 4 层的商人可以覆盖掉该层唯一的战斗。现在检查移到最后，且兜底会避开被保证的商人——只找「非商人」的道是不够的，因为牌池本身会摇出商人，三条道全是商人时 `findIndex` 返回 -1，赋值会静默落空。

地图生成改变，指纹随之变为 `0cd4b619dc55a558d45c7d38d0f6d05ff351f9292f71871787023255a830439e`。已有存档自带地图，不受影响（旧十二伙伴存档仍通过校验）。`node balance.cjs 30` 共 450 局：99 / 79 / 52% → 96 / 79 / 50%，仍在目标区间，未重新标定难度。

### 遗物数值单一来源

遗物是同一类漂移的最后一处：`stats()`、`settlement()`、`grant()` 与商人折扣里的字面量，和描述里的中文各写一遍。现在每件遗物声明 `values` 与模板，`RELIC_VALUES` 是调整强度的唯一位置，规则一律通过 `relicValue(relics, id, key)` 读取（层数 × 单层数值），描述由同一份 values 渲染。

同规格的活链接检查再次抓到一个：`wardflow.cooldown` 驱动 `wardReady = b.time + 1`，而文案写的是「每秒」不含数字，改冷却不会改文案。改为条件渲染——冷却为 1 时仍显示「每秒」，其他值显示具体秒数。检查还额外验证描述所述强度确实是规则施加的强度，且按层数线性叠加（生命与攻击按 `stats()` 的取整比较），以及行商信物的最低价保持为不随层数变化的下限。

17 条遗物描述与改动前逐字节相同。三类合计 58 条文案（23 技能 + 18 装备 + 17 遗物）全部逐字节相同，指纹未变。

`node test.cjs` 现为 75 项，新增四项分别锁定精制装备的缩放范围、技能参数与文案的活链接、地图的分支与保证、以及遗物数值与文案和规则的一致性。

### 公网发布

`20260908-073703-drift-free-rules` 已发布，回退版本 `20260908-064830-balance-and-tooling`。八个公网资源全部 HTTPS 200 且 SHA-256 与本地一致，无需归一化。独立复核：公网 `engine.js`（102284 字节）与 `world.js`（13719 字节）均与本地逐字节相同，线上可见技能参数表、遗物数值表与地图的第二条路保证。本次发布一次通过，上一轮修好的 `deploy.py` 校验路径与 sudo 写入均按预期工作。

## 2026-09-08 · 界面结构重整与浏览器验收

本轮先补安全网再动代码：`game.js` 此前没有任何自动化覆盖，拖拽、暂停、弹窗、存档恢复全靠人眼。

### 浏览器验收

`browser-test.cjs` 用 CDP 驱动真实页面走固定脚本：新建种子远征、酒馆招募、点击上阵、拖拽上阵、刷新与锁定酒馆、扩充人口、穿脱装备、开战中暂停并切速度、打完战斗看战报，再走完两段完整远征（行进、休整、交易、开箱、事件、招募、直到战败），最后刷新验证存档恢复。共 63 个检查点，每点记录引擎状态、各渲染区域 innerHTML 的哈希、界面文案与每个棋子的位置与样式，整份记录取 SHA-256 作为界面指纹。

它自带静态服务器与独立 profile 的 Chrome，不触碰机器上已有的浏览器会话；任何控制台错误或非法远征状态都会让验收失败。战斗时钟按速度 6 运行——战斗按固定步长推进，倍速不改变任何结果。行走脚本会断言自己在每个阶段都能操作，卡住即失败。

### 界面状态收敛

`game.js` 原有 21 个模块级可变标志。现收进一个 `ui` 对象并逐项注释；`state` 保持独立，它是远征而非视图，也是 `index.html` 与 QA 页面读取的对象。

178 处引用**按 ESLint 报出的坐标定点改写**，而不是用正则。这很关键：`mapHTML` 里有同名局部变量 `selected`，字符串 `'selected'` 作为 CSS 类出现 11 次，正则会同时改坏这两者。改写后 ESLint 无告警，即证明没有遗漏。

### 拆分为六个模块

| 模块            | 行数 | 职责                           |
| --------------- | ---: | ------------------------------ |
| `ui-store.js`   |  175 | 存档、共享数据、通知           |
| `ui-effects.js` |  219 | 飘字、光环、弹道、战斗事件播放 |
| `ui-map.js`     |  148 | 路线条、章节地图、服务节点界面 |
| `ui-board.js`   |  523 | 棋盘、面板、选择与移动伙伴     |
| `ui-battle.js`  |   97 | 战斗节拍                       |
| `ui-dialogs.js` |  387 | 全部弹窗                       |
| `game.js`       |  435 | 事件接线、启动、共享绑定       |

函数体整段搬移，未改动一个字符；204 处跨模块引用同样按 ESLint 坐标改写。18 个函数被判定为仅本模块使用，成为私有。

这一步抓到一个真隐患：`moveTo` 同时是 `window` 的方法，因此事件接线里的 5 处调用**不会被 ESLint 报为未定义**，改写后会静默调用浏览器 API 去移动窗口而不是移动棋子。现有一条检查覆盖这一类：模块函数不得与浏览器全局同名、模块不得导出自己未声明的名字、任何 `T.foo()` 都必须有模块导出——最后一条 ESLint 看不见，只有浏览器运行才会暴露。裁剪导出时曾因此静默丢掉 `showRewards`，公开面现已由该检查钉住。

### 冗余重绘

实测一段真实游玩：493 次容器重绘中 **66% 产出的是屏幕上已有的标记**。重写容器会丢弃其 DOM，连同其中的焦点、滚动位置与选择状态——图鉴筛选器此前就得在每次输入后手工重新聚焦。

`paint(id, html)` 仅在标记不同时才写入，28 处 `$('id').innerHTML =` 改为调用它。战斗中的目标箭头是最大单点浪费（116 次写入、115 次相同），改为算好一次再画。重绘次数 493 → 206，其中冗余 323 → 47。

比较的是**实时 DOM 而非缓存的字符串**。第一版用缓存，浏览器验收一轮之内就抓到后果：`renderStatus` 在画完 `#relics` 后又追加了构筑手记按钮，缓存永远匹配不上真实内容，容器不再被重置，按钮每次渲染都累加一个。直接比较节点本身，无论还有谁改动它都正确。

转换由 JavaScript 解析器判定表达式边界，再把每个文件反向还原为赋值形式并要求与输入逐字节一致。

三步改动的界面指纹全程不变：`d8713a0949720d81da859a05ad8edc85040d58a8b06db83eb4a62664240bce7c`。`npm run verify` 现在以浏览器验收收尾，规则检查 76 项。

## 2026-09-08 章节美术重构

- 三张独立手绘场景已接入实际节点主题：苔林边境、沉星回廊、长夜之心。场景路径集中在 CSS 变量；移除旧滤色，使用各章原生配色和暗角，终局复用月夜场景。
- PNG 原图保留，发布只包含三张 1536×1024 WebP，总计 947174 字节，较 PNG 减少约 89.7%。旧背景保留在源码，不进入新发布包。
- `npm run check`、`npm run lint`、`npm run format:check` 通过；`npm test` 76 项通过。
- `CHROME_PATH='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome' npm run browser-test -- --art-dir=/tmp/tundra-art-20260908` 通过。所有交互检查点状态有效，控制台零错误；UI 指纹 `d8713a0949720d81da859a05ad8edc85040d58a8b06db83eb4a62664240bce7c`。
- 新增可选 `--art-dir` 验收入口：由真实章节节点驱动渲染，验证对应背景路径、浏览器解码尺寸、发布清单及无横向溢出，并保存截图。未指定时保持原有交互回归流程。
- 人工逐张检查 forest / ruin / moon 在 1366×900 和 390×844 的六张截图：棋子、星级与格子边界可辨，中央无遮挡棋盘的装饰主体，手机裁切正常。
- Ego Lite 成功加载和解码三张场景；其截图接口超时，目视验收使用上述独立 headless Chrome 截图完成。
- 本轮完成本地改动与验收，未执行 commit、push 或线上部署。

## 2026-09-08 美术发布验收

美术提交 `7d7c84d` 已推送，线上切换到 `20260908-222839-chapter-art`，回退保留 `20260908-073703-drift-free-rules`。发布时 76 项规则检查与语法、静态、格式检查通过，源站全部文件哈希一致。

终端直连被企业网络过滤重定向，发布脚本在公网核验阶段退出。Ego Lite 后续逐项核对 release-files.json 中 16 个公网资源：均返回 HTTPS 200 且 SHA-256 与本地完全一致；首页加载新森林背景，三张场景均正常解码为 1536×1024，界面模块可用、无横向溢出。已据独立证据补齐 .last-release.json 与发布文档，没有重复切换或修改网络设置。

## 2026-09-09 角色与棋盘融合

- 22 位伙伴及古树首领保留独立轮廓，全部接入左上受光的部件材质渐变与专属装备刻线；守卫护甲、游侠斗篷、法师长袍增加曲线。色阶模板初始化一次，实例仅替换 SVG ID；未增加运行时依赖或图像下载。
- 棋盘移除厚圆盘及重复投影，使用柔和接触阴影和薄队伍环；己方实线、敌方虚线，选中和键盘聚焦使用暖色亮环。二/三星轮廓保留，三章分别提供暖金、灰绿、银蓝细节反光。
- `npm run check`、`npm run lint`、`npm run format:check`、`git diff --check` 通过；`npm test` 76 项通过。
- `CHROME_PATH='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome' npm run browser-test -- --art-dir=/tmp/tundra-figures-20260909` 完整回归通过，所有交互检查点状态有效，控制台零错误。UI 指纹为 `90fc8289b502ee86f9583d6ee83eb4342b06d2729742b7d281ba826b84278639`，角色 SVG 更新会改变 DOM 指纹，不作为语义等价声明。
- 美术验收包含 23 个角色的图册，以及守卫、游侠、法师在三章 1366×900 和 390×844 中的上阵与选中画面，共七张截图，均已目视检查。无横向溢出；SVG XML 均能解析，渐变引用全部存在，页面 SVG ID 无重名。
- 本轮为本地实现与验收，尚未提交或部署；线上仍以 .last-release.json 为准。

## 2026-09-09 战斗反馈与视觉统一 · 本地完成

- 物理/法术/真实伤害、治疗、护盾、破盾使用不同形状与颜色；魔法弹道沿用角色配色。部分护盾吸收同时显示扣血和护盾损耗，保留正负号与文字。
- 同格最多三条飘字，整层最多 32 条飘字与 64 个 SVG 瞬态效果；提前淘汰时取消动画，暂停时新建动画也暂停。游戏减动画和系统减少动态效果均抑制瞬态反馈。
- 酒馆、图鉴、选择卡片与装备区域统一深绿材质、圆角、边框及聚焦样式；八类常用功能图标使用同一套线性 SVG，按钮保留文字或 aria-label。
- 76 项规则检查、语法检查、ESLint、Prettier 及差异空白检查通过。完整独立浏览器回归全部状态有效、控制台零错误；UI 指纹 `40da17a175cac6e43a9c336975b68999457192b86edb2f3c58b2b87a61bf37e8`。角色/图标改变 DOM，因此不作为语义等价指纹。
- 完整验收命令：`CHROME_PATH='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome' npm run browser-test -- --art-dir=/tmp/tundra-feedback-20260909`。包括全部角色 SVG、三章桌面/手机截图与反馈专项。
- 快速反馈验收：同命令增加 `--feedback-only`。受控事件验证部分吸收双数值、五种示例形状、暂停、连续 90 次命中的密度限制，以及游戏内和系统减动画。保存桌面和手机反馈示例截图；这些截图是受控事件展示，不是伤害计算记录。
- 图鉴桌面与开局手机面板另行截图目视检查，无横向溢出。前一轮角色和棋盘融合改动保留。
- 验收环境：一次战斗等待超时后为测试 Chrome 禁用后台计时节流；新增动画检查显式模拟 no-preference/reduce，避免本机系统减少动态效果使正常动画样例为空。最终完整回归与专项均通过，未修改用户系统设置。
- 本轮尚未提交、推送或部署，线上版本仍以 .last-release.json 为准。

## 2026-09-09 章节氛围延伸 · 本地完成

- 三章配色、场景延伸到营地、商人、事件、宝箱、地图、精制弹窗和首领侦察。新增章节短叙事，规则数据及计算未改。
- 专项命令：`CHROME_PATH='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome' npm run browser-test -- --chapter-only --art-dir=/tmp/tundra-chapters-20260909`。覆盖三章六类页面，在 1366×900 / 390×844 共保存 36 张截图；验证跨章地图配色、未知事件不泄露、四件真实商人物品及无横向页面溢出，控制台零错误。种子节点经真实 enterNode 初始化，截图不代表已完成整段远征。
- 目视检查桌面地图、商人、首领侦察和手机营地、商人、地图；商人物品在手机旅途面板内纵向滚动，地图在独立区域横向滚动。
- 前两轮本地角色和战斗反馈修改保留；本轮仍未提交、推送或部署。

- 最终验证：76 项规则测试、语法、ESLint、Prettier 与 `git diff --check` 均通过。完整浏览器回归检查点全部有效，控制台零错误；UI 指纹为 `4fde9e3e9a000aeea0c2eea88897cada03a9174cc00d93484b787a70d7890161`（本轮标题及叙事文字改变 DOM）。

## 2026-09-09 · 三十位伙伴、羁绊档次与构筑深度

本轮改的是策略层：阵容 22 → 30，羁绊由「阵营 2 / 3、职业 2」改为多档并解锁新行为，加入纹章、标记、削弱、斩杀、技能暴击、护盾反弹与攻速增益，人口上限 6 → 7，构筑组合 4 → 7。战斗循环、经济结构、地图生成与存档格式没有改。

### 规则检查

`node test.cjs`：85 项通过（上一轮 76）。新增九项覆盖本轮机制：

- 羁绊按档次逐级支付，超过最高档不再增加；职业加成只发给对应职业（守卫的回复不会发给猎手）。
- 纹章只为佩戴者补一个阵营：本身已属于该阵营时不重复计入，两名同种伙伴戴同一枚纹章仍只算一次；纹章不进掉落池、不能在营地精制。
- 标记只保留最强的一层但取最长的持续时间，过期后停止放大；削弱记在攻击方身上，降低它造成的全部伤害。
- 斩杀线与猎影印记相乘；两面互相反弹的壁垒会收敛而不是无限弹射，护盾耗尽后壁垒停止反弹。
- 余烬四层的溅射、追风三层的真实射击、战鼓的攻速增益都发生在普攻这一步，且增益不改写伙伴自身的攻击间隔。
- 技能暴击按暴击倍率放大整个技能；月影四层与棱光石取较高值，两枚棱光石封顶为必定暴击。
- 八位新伙伴各自命中其描述承诺的目标（整列贯穿、最远目标加标记、护盾最薄的三人、最近的两名友军回蓝），并各自完成一场可存档续接的确定性战斗。
- 七人阵容能合法落位、通过存档校验并开战；商人按「离进阶最近」的阵营出售纹章。
- 地图能掷出的每个词缀都在规则里有对应实现（此前只靠人工核对），新词缀「回响之地」「灼印」按其文案改变敌人。
- 被篡改的战斗快照不能塞进永久或超界的标记、削弱、壁垒、鼓点与号角层数。

### 模拟

`node balance.cjs 30`：五开局 × 三难度 × 30 种子，共 450 局，全部正常结束且状态合法。

| 难度 |             通关 | 各开局区间 |
| ---- | ---------------: | ---------- |
| 悠游 | 145 / 150（97%） | 27–30 / 30 |
| 冒险 | 127 / 150（85%） | 20–29 / 30 |
| 险境 |  75 / 150（50%） | 10–18 / 30 |

这里有一处必须说明的方法学修正：旧 `balance.cjs` 给阵容打分时只看生命与每秒伤害，护甲、护盾与治疗全部计零，因此它会系统性低估潮汐这类保护型阵营——上一版数据里潮汐险境 7 / 30、余烬 26 / 30 的差距，有相当部分是**测量的盲点**而不是设计的强弱。把护甲、开场护盾与治疗加成计入评分后，险境的开局区间从 7–28 收敛到 10–18。这仍是固定启发式策略的结果，不是真人胜率。

敌人强度按新的羁绊档次重新标定：敌群同样吃 2 / 3 / 4 档加成，因此各章倍率整体下调，第三章后段扩到七人、第三章首领多一名前排。

### 指纹

规则指纹与界面指纹都按预期改变（本轮是规则改动，不是重构）：

- `node fingerprint.cjs`：`64e59b005819e6449f806c72145e4cd6bdd7cd0b93e65da8a8f2c537ed6ea7e8`。指纹的取样阵容同时扩到七人并纳入纹章，因为纹章会改变一支队伍实际拥有的羁绊。
- `npm run browser-test`：63 个检查点全部合法，控制台无错误，界面指纹 `f7066a97c6aa5046ba8970f651e91a5c8d8b255a4ca6f323331a1e92268e0c9c`。走图脚本在第二章实际把队伍带到七人并完成战斗、交易、休整与刷新恢复。
- 浏览器验收本身补了一处：页面报错此前只在跑完全程后才打印，中途失败时会被超时信息盖掉；现在早退也会输出控制台错误。

### 界面

- 羁绊面板按档次显示 `n / 下一档`，达成的档次用不同亮度的徽章区分，悬停显示全部档次；羁绊详情页对阵营和职业统一列出每一档，并标出当前所处档次与该阵营的纹章。
- 新技能接入既有表现：贯穿箭沿整列画出光圈，标记以「✦ 易伤 +25%」飘字呈现，护盾类新技能使用护盾音效而不是攻击光圈。
- 装备比较新增魔法减伤、每层攻击与技能易伤三项，换装前后仍逐项显示增减。
- `tests/roster30-qa.html` 为本轮的隔离检查页（`?view=art` / `codex` / `builds` / `trait&id=moon`），使用内存存储，不发布到公网、不覆盖玩家存档。

### 兼容

新远征使用 `companions-30`。`companions-22` 与 `twelve-1` 的存档、战斗快照与记录继续可读：已经上路的远征保留已生成的地图，按当前规则走完；旧分享链接仍提示版本差异。
